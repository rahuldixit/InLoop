version: 2
jobs:
  build:
    docker:
      - image: cypress/base:8
        environment:
          ## this enables colors in the output
          TERM: xterm
    working_directory: ~/cypress
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-deps-master-package.json
            - v1-deps-master
            - v1-deps
      - run:
          name: Install Dependencies
          command: npm ci
      - save_cache:
          key: v1-deps-master-package.json
          # cache NPM modules and the folder with the Cypress binary
          paths:
            - ~/.npm
            - ~/.cache
      - run: mkdir ~/reports  
      - run: mkdir ~/artifacts  
      - run:
          command: npm run cypress #run custom command to clean reports, run tests, merge and generate reports
      - store_test_results:
          path: ~/reports
      - store_artifacts:
          path: ~/artifacts  